# Auto-activate venv when you enter the repo
source .venv/bin/activate

# Load environment variables for the app
# (If you prefer, you can export them directly here instead of relying on .env)
# export LLM_PROVIDER=openai
# export OPENAI_API_KEY=your_key_here
# export OPENAI_MODEL=gpt-5
# export OPENAI_EMBED_MODEL=text-embedding-3-large
# export DOCS_DIR=./data/docs
# export INDEX_PATH=./data/index.json

